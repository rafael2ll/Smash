<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_height="match_parent"
	android:layout_width="match_parent">

	<AutoCompleteTextView
		android:layout_height="wrap_content"
		android:inputType="textPersonName"
		android:ems="10"
		android:layout_width="match_parent"
		android:id="@+id/simpledialogEditTextNome"
		android:hint="Nome"/>

	<Spinner
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:layout_below="@id/simpledialogEditTextNome"
		android:layout_centerVertical="true"
		android:layout_centerHorizontal="true"
		android:id="@+id/simpledialogSpinner"/>

	<CheckBox
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:layout_below="@id/simpledialogSpinner"
		android:text="Single"
		android:textSize="17sp"
		android:layout_margin="10dp"
		android:id="@+id/simpledialogCheckBoxIsSingle"/>

</RelativeLayout>

